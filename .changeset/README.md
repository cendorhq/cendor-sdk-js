# Changesets

Run `pnpm changeset` to record a version bump for `@cendor/sdk`. CI opens a version PR and, on merge,
publishes to npm with provenance via OIDC trusted publishing (tag ≍ version asserted).
